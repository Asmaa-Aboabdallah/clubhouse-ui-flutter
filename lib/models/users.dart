
class User {
  final String firstName;
  final String lastName;
  final String imageUrl;

  const User({
   required this.firstName, // must assign value
   required this.lastName, // must assign value
   required this.imageUrl, // must assign value
});
}
